# Forward Pass Log

One line per finding fixed during gate forward passes: gate, finding, fix, commit.

- G1 · `/admin/users` returned 200 (empty list) to non-admins because index relied on policy_scope alone · added class-level `authorize User` on index + integration test · (G1 commit)
- G1 · `case[status]` could be mass-assigned around the state machine · status excluded from permitted params, model-level guard validates all status changes go through `transition_to!` + tests · (G1 commit)
- G1 · staff composer could forge `agent_turn` messages · MessagesController whitelists kind to public_reply/internal_note + test · (G1 commit)
- G1 · deactivated/soft-deleted users could resume existing sessions · `find_session_by_cookie` now checks `user.active?` (soft-deleted users vanish via default scope); `deactivate!` destroys sessions + tests · (G1 commit)
- G1 · `dependent: :nullify` on soft-deleting parents would have NULLed `cases.contact_id` (NOT NULL) and erased history · associations changed to `restrict_with_error` (contact↔cases) / no-dependent + `with_deleted` belongs_to scopes + tests · (G1 commit)
- G1 · Brakeman: role mass-assignment in Admin::UsersController · justified ignore (admin-only controller, Pundit-enforced, tested) in `config/brakeman.ignore` · (G1 commit)
- G2 · email threading trusted the tracking ID alone — anyone who learned an ID could inject into the thread · sender address must match the case contact, otherwise the mail opens its own case + test · (G2 commit)
- G2 · `Rails::HTML5::FullSanitizer` kept `<script>` text content from HTML-only emails · switched to Loofah `:prune` scrub + test asserting script payloads are gone · (G2 commit)
- G2 · portal tracking lookup could become an enumeration oracle · identical generic error for unknown-ID vs wrong-contact (tested), 28^8 ID space, Rack::Attack throttles on lookup + submission · (G2 commit)
- G2 · internal notes could have leaked to citizens via the status page · portal thread explicitly scopes to public_reply/agent_turn kinds + test · (G2 commit)
- G2 · CSV export was formula-injectable via user-controlled names · `csv_safe` quotes leading =,+,-,@,tab · (G2 commit)
- G2 · file-upload abuse on portal/composer/email · shared AttachableValidation (type allowlist, 10MB cap, max 5) on every surface; mailbox drops disallowed attachments + tests · (G2 commit)
- G2 · confirmation page renders (201, no redirect) so tracking IDs never enter URLs/logs/Referer · (G2 commit)
- G3 · service accounts could have managed identity (users/tokens/service accounts) — privilege escalation · those endpoints are human-admin-token-only; no scope opens them + tests · (G3 commit)
- G3 · on-behalf-of without contacts:write would let a cases:write integration mint contacts · explicit scope check on OBO resolution + test · (G3 commit)
- G3 · expired/rotated/deactivated credentials kept working in early draft · access-token expiry checks, rotation revokes live tokens, deactivation deletes tokens — all tested · (G3 commit)
- G3 · settings API echoed the BYOK key back to admins · GET /settings masks secrets ([SET]) + test asserting raw value absent · (G3 commit)
- G3 · agent_turn forgery via API composer (same as console) · kind whitelist in API messages controller + test · (G3 commit)
- G3 · Brakeman: webhook URL regex lacked \z anchor · replaced regex with URI.parse + URI::HTTP class check (also blocks newline tricks) · (G3 commit)
- G3 · Brakeman: role mass-assignment in API users controller · justified ignore (admin-human-only, tested) · (G3 commit)
- G3 · customer SSO plane separation verified adversarially: customer session bounced from staff console and other contacts' portal cases 404 + tests · (G3 commit)
