<turbo-frame id="<%= defined?(frame_id) ? frame_id : "assist-summary" %>">
  <div class="assist-output flash-alert" role="alert">
    <p><%= t("assist.failed") %></p>
    <p class="muted text-sm"><%= message %></p>
  </div>
</turbo-frame>
