# Known Gaps

Stubbed or deferred items, with severity. Empty is the goal.

- ~~Keycloak live-flow tests not executed in the build container~~ **Executed 2026-06-10 on a real Docker host — found and fixed three stacked bugs** (swd-forced HTTPS discovery; Turbo intercepting the SSO form POST; CSP `form-action 'self'` blocking the IdP redirect — the latter two were real product bugs invisible to the test-mode suite) plus a realm fixture that referenced undefined client scopes. Both live flows now pass: `bin/keycloak-test`. SAML remains test-mode only (Keycloak SAML client config not included).
- ~~`docker compose build` not executed in the build container~~ **Executed 2026-06-10 on a real Docker host** — image built clean, Postgres prepared/seeded, full 12-check smoke passed against the compose instance (`SMOKE_BASE_URL=… bin/smoke`; remote-target mode needed a fix: the API token is now minted inside the app container).
- **Email intake requires deployment-side ingress configuration** (severity: info, by design). Action Mailbox processing, threading, anti-spoofing, and attachment filtering are implemented and tested; wiring an SMTP relay/ingress (e.g. Postfix → `/rails/action_mailbox/relay/inbound_emails`) is environment-specific and documented in the Rails guides — not something a self-contained compose file can do for you.
- **Accessibility: automated coverage only** (severity: info). axe-core (WCAG 2.1 A/AA rule sets) runs in the system suite against ten key surfaces with zero violations; a manual screen-reader walkthrough (NVDA/VoiceOver) by a human tester remains worthwhile before a GIGW-audited deployment.

## AI / connector effector layer (opt-in, off the deploy critical path)

The connector framework is an agent **effector layer** (invoke/scope/approval + budgeted autonomy + delegation-ID + decision-class). It is fully unit/integration tested, but the following are validated against test doubles rather than the live world:

- **Connectors tested against stubs, not live external APIs** (severity: info — the largest current gap). The catalogue is **57 providers** — 45 static-credential + a 12-provider OAuth2 tier (see the next bullet). The static-credential set is the original 4 + 38 commercial-lane providers built 2026-06-13 + the 3 Netcore per-channel providers (Email, WhatsApp, SMS): WhatsApp, Shopify, Stripe, HubSpot, Zendesk, SendGrid, Twilio, Telegram, Teams/Google-Chat/Zapier/Make/n8n webhooks, Gupshup/Plivo/Exotel/Kaleyra/Sinch, Cashfree, Freshdesk, Intercom, Pipedrive, Freshsales, Mailchimp, WooCommerce, Typeform, Jotform, ServiceNow, Jira, monday, Asana, ClickUp, Trello, Notion, Airtable, ActiveCampaign, Klaviyo, Mailgun, Netcore Email/WhatsApp/SMS. Every provider has a unit test, but each is implemented against the **documented** API shape (endpoint, auth header, body) and stubbed at `Net::HTTP`. On 2026-06-13 the 35 real-API providers were **doc-verified** (an adversarial pass checking base/path/method/auth/body against each vendor's current official docs — 29 clean, 2 minor, 4 corrected: pipedrive `/api` path, sinch region host, gupshup `/sm`→`/wa` EOL migration, shopify fulfillment-order flow). But **none has been exercised against a live endpoint with real credentials** — doc-verified ≠ live-tested. Validate each against the live API (and re-check time-sensitive details, e.g. WhatsApp pricing, Shopify REST→GraphQL, monday API-Version) before relying on it in production.
- **OAuth2 tier: 12 providers built, none live-validated** (severity: info). The OAuth2 authorization-code + transparent-refresh seam shipped 2026-06-13 (`Connectors::OauthProvider` + admin connect/callback, commit `6e9c903`). The tier now has **12 providers**: Google Calendar (reference), Gmail, Google Sheets, Google Drive, Microsoft 365 (Outlook mail + calendar), Salesforce, Zoho CRM, HubSpot (OAuth), Dynamics 365, DocuSign, Box, Dropbox. Each is unit-tested with the token exchange/refresh and each action's wire call stubbed at `Net::HTTP` — same **doc-shaped, not live-validated** caveat as the static-credential set (no real OAuth app has been connected end to end; per-provider wrinkles worth live-checking: Salesforce/Zoho persist a per-account API host from the token response, Zoho uses a `Zoho-oauthtoken` header, Dynamics needs a resource-scoped `.default` grant). Remaining roadmap OAuth connectors not yet built: Microsoft Teams / SharePoint / OneDrive, Zoom, Marketo, Adobe Acrobat Sign, Google Ads / LinkedIn, etc. Gov DPI connectors remain licence-gated (see the connector-lane decision).
- **Netcore SMS endpoint is an unverified working assumption** (severity: info). Netcore Email + WhatsApp are built to confirmed doc contracts (Email retries `Authorization: x-api-key` → `bearer` at runtime to settle the documented header disagreement). Netcore SMS uses the CPaaS-Deflector multi-channel send as a working assumption — the standalone-SMS host / method / field-names could not be extracted from Netcore's client-rendered docs, and the auth header (`api-key` → `api_key`) is tried both ways. Settle with one authenticated live call (see `plan/netcore-research-2026-06-13.md`) before production use; the DLT template/sender requirements are confirmed.
- **Agent dispatch loop run only with the `fake` LLM client** (severity: info). `Connectors::AgentRunner` + `Llm::HttpClient#chat_with_tools` are driven by `Llm::FakeClient` in tests; the end-to-end "tool-use turn → `Connectors::Invoke.call`" loop has not been run against a real model endpoint.
- **Decision-class doctrine is judicially untested for AI in India** (severity: info). The autonomous / confirm / of_record gating is grounded in administrative-law principles (reasoned-order duty, non-fettering, audi alteram partem), not in case law applying them to AI-influenced decisions — monitor (e.g. SC Draft AI Regs for Courts 2026).
